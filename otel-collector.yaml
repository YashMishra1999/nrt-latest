receivers:
  otlp:
    protocols:
      grpc:
      http:

exporters:
  otlphttp/bridge:
    endpoint: http://bridge:9000   # ✅ FIXED
    tls:
      insecure: true

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [otlphttp/bridge]
